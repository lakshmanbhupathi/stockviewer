spring:
  application:
    name: db-service

server:
  port: 8300

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8302/eureka
    instance:
      hostname: localhost
